[Game]
; Settings for the games Mantella supports.

; game
;   Choose the game to run with Mantella.
;   Options: Skyrim, SkyrimVR, Fallout4, Fallout4VR
;   default = SkyrimVR
game = Skyrim

; skyrim_mod_folder
;   If you are using Mod Organizer 2, this path can be found by right-clicking the Mantella mod in your mod list and selecting 'Open in Explorer'.
;           If you are using Vortex, this path needs to be set to your Skyrim Special Edition\Data folder.
;           eg C:\Games\Steam\steamapps\common\Skyrim Special Edition\Data.
;           If this path is incorrect, NPCs will say the same voiceline on repeat.
;   Must be a valid system path. Target folder must contain folder 'Sound'!
;   default = C:\Modding\MO2\Skyrim\mods\Mantella
skyrim_mod_folder = D:\Games\skyrimmods\MO2\mods\Mantella

; skyrimvr_mod_folder
;   If you are using Mod Organizer 2, this path can be found by right-clicking the Mantella mod in your mod list and selecting 'Open in Explorer'.
;           If you are using Vortex, this path needs to be set to your Skyrim VR\Data folder.
;           eg C:\Games\Steam\steamapps\common\Skyrim VR\Data.
;           If this path is incorrect, NPCs will say the same voiceline on repeat.
;   Must be a valid system path. Target folder must contain folder 'Sound'!
;   default = C:\Modding\MO2\SkyrimVR\mods\Mantella
skyrimvr_mod_folder = D:\Games\skyrimmods\MO2\mods\Mantella

; fallout4_mod_folder
;   If you are using Mod Organizer 2, this path can be found by right-clicking the Mantella mod in your mod list and selecting 'Open in Explorer'.
;           If you are using Vortex, this path needs to be set to your Fallout 4\Data folder.
;           eg C:\Games\Steam\steamapps\common\Fallout 4\Data.
;           If this path is incorrect, NPCs will say the same voiceline on repeat.
;   Must be a valid system path. Target folder must contain folder 'Sound'!
;   default = C:\Modding\MO2\Fallout4\mods\Mantella
fallout4_mod_folder = C:\Modding\MO2\Fallout4\mods\Mantella

; fallout4vr_mod_folder
;   If you are using Mod Organizer 2, this path can be found by right-clicking the Mantella mod in your mod list and selecting 'Open in Explorer'.
;           If you are using Vortex, this path needs to be set to your Fallout 4 VR\Data folder.
;           eg C:\Games\Steam\steamapps\common\Fallout 4 VR\Data.
;           If this path is incorrect, NPCs will say the same voiceline on repeat.
;   Must be a valid system path. Target folder must contain folder 'Sound'!
;   default = C:\Modding\MO2\Fallout4VR\mods\Mantella
fallout4vr_mod_folder = C:\Modding\MO2\Fallout4VR\mods\Mantella

; fallout4_folder
;   The filepath of the Fallout 4 (desktop) directory where the executable is located.
;                           This path only needs to be set if `Vision`->`Use Game Screenshots` is enabled.
;   Must be a valid system path. Target folder must contain file 'Fallout4.exe'!
;   default = C:\Games\Steam\steamapps\common\Fallout 4
fallout4_folder = C:\Games\Steam\steamapps\common\Fallout 4

; fallout4vr_folder
;   The filepath of the Fallout 4 VR directory where the executable is located.
;                           This path only needs to be set if `Vision`->`Use Game Screenshots` is enabled.
;   Must be a valid system path. Target folder must contain file 'Fallout4VR.exe'!
;   default = C:\Games\Steam\steamapps\common\Fallout4VR
fallout4vr_folder = C:\Games\Steam\steamapps\common\Fallout4VR

[LLM]
; Settings for the LLM providers and the LLMs themselves.

; llm_api
;   Selects the LLM service to connect to (either local or via an API).
;   
;               If you are connecting to a local service (KoboldCpp, textgenwebui etc), please ensure that the service is running and a model is loaded. You can also ignore the dropdown options and instead enter a custom URL to connect to other LLM services that provide an OpenAI compatible endpoint.
;               After selecting a service, select the model using the option below. Press the *Update* button to load a list of models available from the service.
;   
;               If you are using an API (OpenAI, OpenRouter, etc) ensure you have the correct secret key set in `GPT_SECRET_KEY.txt` for the respective service you are using.
;   Options: OpenRouter, OpenAI, NanoGPT, KoboldCpp, textgenwebui
;   default = OpenRouter
llm_api = http://localhost:8000/v1

; model
;   Select the model to use. Press the *Update* button to load a list of models available from the service selected above.
;                               The list does not provide all details about the models. For additional information please refer to the corresponding sites:
;                               - OpenRouter: https://openrouter.ai/docs#models
;                               - OpenAI: https://platform.openai.com/docs/models https://openai.com/api/pricing/
;   Options: Custom Model
;   default = google/gemma-4-26b-a4b-it:free
model = openai/gpt-oss-20b

; max_response_sentences_single
;   The maximum number of sentences returned by the LLM on each response in a player<->NPC conversation. Lower this value to reduce waffling.
;   Note: The setting 'Number Words TTS' in the Text-to-Speech tab takes precedence over this setting.
;   max_response_sentences_single must be an integer between 1 and 999
;   default = 4
max_response_sentences_single = 4

; max_response_sentences_multi
;   The maximum number of sentences returned by the LLM on each response in a player<->multi-NPC conversation. Lower this value to reduce waffling.
;   Note: The setting 'Number Words TTS' in the Text-to-Speech tab takes precedence over this setting.
;   max_response_sentences_multi must be an integer between 1 and 999
;   default = 12
max_response_sentences_multi = 12

; custom_token_count
;   If the model chosen is not recognised by Mantella, the token count for the given model will default to this number.
;                       If this is not the correct token count for your chosen model, you can change it here.
;                       Keep in mind that if this number is greater than the actual token count of the model, then Mantella will crash if a given conversation exceeds the model's token limit.
;   custom_token_count must be an integer between 4096 and 9999999
;   default = 4096
custom_token_count = 32768

; wait_time_buffer
;   Time to wait (in seconds) before generating the next voiceline.
;                           Mantella waits for the duration of a given voiceline's .wav file + an extra buffer to account for processing overhead.
;                           If you are noticing that some voicelines are not being said in-game, try increasing this buffer.
;   wait_time_buffer must be a floating point number between -999 and 999
;   default = 0
wait_time_buffer = 0.0

; llm_params
;   Parameters passed as part of the request to the LLM.
;                           A list of the most common parameters can be found here: https://openrouter.ai/docs/parameters.
;                           Note that available parameters can vary per LLM provider.
;   default = {
;       "max_tokens": 250,
;       "stop": ["#"]
;   }
llm_params = "{\n    \"max_tokens\": 250,\n    \"stop\": [\"\#\"]\n}"

; narration_handling
;   How to handle narrations in the output of the LLM.
;                                               - Cut narrations: Removes narrations from the output.
;                                               - Respective character speaks its narrations: The currently active character will speak it's actions out aloud.
;                                               - Use narrator: Narrations will be spoken by a special narrator. The voice model can be set by the config value 'Narrator voice' below.
;                                               - Deactivate handling of narrations: Any narration or speech indicators will be ignored during parsing.
;   
;                                               Note: The seperation of narration and speech is experimental and may not work if the LLM output is not formatted well.
;   Options: Cut narrations, Respective character speaks its narrations, Use narrator, Deactivate handling of narrations
;   default = Cut narrations
narration_handling = Cut narrations

; narrator_voice
;   Which voice model to use if 'Narration Handling' is set to 'Use narrator'.
;                           Must be a valid voice model from the current TTS. Same rules apply as for choosing a voice for the player.
narrator_voice =

; narration_start_indicators
;   List of characters used to identify the start of narrations in the LLM output.
;   Options: *, (, [
;   default = *, (, [
narration_start_indicators = *, (, [

; narration_end_indicators
;   List of characters used to identify the start of narrations in the LLM output.
;   Options: *, ), ]
;   default = *, ), ]
narration_end_indicators = *, ), ]

; speech_start_indicators
;   List of characters used to identify the end of narrations in the LLM output.
;   Options: "
;   default = "
speech_start_indicators = "

; speech_end_indicators
;   List of characters used to identify the start of speech in the LLM output.
;   Options: "
;   default = "
speech_end_indicators = "

; narration_indicators
;   Which narration indicators to use for sentences identified as narrations.
;                           If sentences get marked as narrations and are not cut, they will be surrounded by these narration indicators the next time they are fed back to the LLM.
;                           This helps to keep the LLM consistent in its use of narration indicators.
;   Options: (), **, []
;   default = ()
narration_indicators = ()

; claude_prompt_caching_enabled
;   WARNING: This is an experimental feature and may increase rather than decrease billing.
;                           Enable Claude prompt caching when using Anthropic Claude models via OpenRouter.
;   claude_prompt_caching_enabled must either be 'True' or 'False'
;   default = False
claude_prompt_caching_enabled = False

; allow_per_character_llm_overrides
;   Allow individual characters to use different LLM models/providers for one-on-one conversations.
;               To use this feature, add 'llm_service' and 'model' values under the appropriate columns in the characters CSV file.
;               Characters without these columns will fall back to the global LLM settings.
;               This feature only applies to one-on-one conversations, not multi-NPC.
;   allow_per_character_llm_overrides must either be 'True' or 'False'
;   default = False
allow_per_character_llm_overrides = False

; summary_llm_enabled
;   Enable a separate LLM client for generating conversation summaries.
;               When enabled, the Summary LLM settings below will be used instead of the main LLM settings.
;               When disabled, summaries use the same LLM as conversations.
;   summary_llm_enabled must either be 'True' or 'False'
;   default = False
summary_llm_enabled = False

; summary_llm_api
;   Selects the LLM service to use for generating conversation summaries.
;   
;               If you are connecting to a local service (KoboldCpp, textgenwebui etc), please ensure that the service is running and a model is loaded. You can also ignore the dropdown options and instead enter a custom URL to connect to other LLM services that provide an OpenAI compatible endpoint.
;               After selecting a service, select the model using the option below. Press the *Update* button to load a list of models available from the service.
;   
;               If you are using an API (OpenAI, OpenRouter, etc) ensure you have the correct secret key set in `GPT_SECRET_KEY.txt` for the respective service you are using.
;   
;               By default, summaries use the same LLM as conversations. Configure this to use a different model for summaries.
;   Options: OpenRouter, OpenAI, NanoGPT, KoboldCpp, textgenwebui
;   default = OpenRouter
summary_llm_api = OpenAI

; summary_llm
;   Select the model to use for generating conversation summaries. Press the *Update* button to load a list of models available from the service selected above.
;                               You can use a different model for summaries than for conversations.
;                               The list does not provide all details about the models. For additional information please refer to the corresponding sites:
;                               - OpenRouter: https://openrouter.ai/docs#models
;                               - OpenAI: https://platform.openai.com/docs/models https://openai.com/api/pricing/
;   Options: Custom Model
;   default = google/gemma-4-26b-a4b-it:free
summary_llm = No secret key found for OpenRouter

; summary_custom_token_count
;   If the summary model chosen is not recognised by Mantella, the token count for the given model will default to this number.
;                       If this is not the correct token count for your chosen model, you can change it here.
;                       Keep in mind that if this number is greater than the actual token count of the model, then Mantella may crash if a given conversation exceeds the model's token limit.
;   summary_custom_token_count must be an integer between 4096 and 9999999
;   default = 4096
summary_custom_token_count = 4096

; summary_llm_params
;   Parameters passed as part of the request to the LLM.
;                           A list of the most common parameters can be found here: https://openrouter.ai/docs/parameters.
;                           Note that available parameters can vary per LLM provider.
;   default = {
;       "stop": ["#"]
;   }
summary_llm_params = "{\n    \"stop\": [\"\#\"]\n}"

; random_llm_enabled
;   Enable random LLM selection. When enabled, a random LLM will be picked from the pool at conversation start.
;   random_llm_enabled must either be 'True' or 'False'
;   default = False
random_llm_enabled = False

; random_llm_pool
;   JSON array of LLMs for random selection.
;                           Format: [{"service": "OpenRouter", "model": "google/gemma-4-26b-a4b-it:free"}, ...]
;   default = []
random_llm_pool = []

[TTS]
; Settings for the TTS methods Mantella supports.

; tts_service
;   The TTS service used by Mantella.
;   Options: Piper, XTTS, xVASynth
;   default = Piper
tts_service = Piper

; xvasynth_folder
;   The folder you have xVASynth downloaded to (the folder that contains xVASynth.exe).
;   Selected folder must contain subfolder '\resources\'!
;   Must be a valid system path. Target folder must contain file 'xVASynth.exe'!
;   default = C:\Games\Steam\steamapps\common\xVASynth
xvasynth_folder = C:\Games\Steam\steamapps\common\xVASynth

; xtts_server_folder
;   The folder you have the XTTS server downloaded to (the folder that contains xtts-api-server-mantella.exe).
;   Must be a valid system path. Target folder must contain file 'xtts-api-server-mantella.exe'!
;   default = C:\Games\Steam\steamapps\common\XTTS
xtts_server_folder = C:\Games\Steam\steamapps\common\XTTS

; piper_folder
;   The folder where Piper is installed (where piper.exe exists).
piper_folder =

; lipgen_folder
;   The LipGenerator program is required by Bethesda games to accurately lip sync voicelines.
;                           Please install the Creation Kit from Steam. Once installed, set this path to be the same path as your game folder (where the executable is located).
lipgen_folder = D:\SteamLibrary\steamapps\common\Skyrim Special Edition\Tools\LipGen\LipGenerator

; facefx_folder
;   The FaceFXWrapper is an alternative program to LipGenerator. Lip sync generation is required by Bethesda games to accurately lip sync voicelines.
;                                   This path does not need to be set if you have the LipGenerator folder configured.
facefx_folder =

; number_words_tts
;   Minimum threshold of words in a sentence needed before sending to the TTS service.
;                           TTS services often struggle with synthesizing short voicelines, so increasing this threshold can improve quality.
;   number_words_tts must be an integer between 1 and 999999
;   default = 3
number_words_tts = 3

; lip_generation
;   Whether to generate lip sync files for spoken voicelines. Disable this setting to improve response times.
;                           Set to 'Lazy' to skip lip syncing only for the first sentence spoken of every response.
;   Options: Enabled, Lazy, Disabled
;   default = Enabled
lip_generation = Enabled

; fast_response_mode
;   Whether to play the first voiceline of every response directly from the Mantella exe instead of in-game (Skyrim only).
;                           Enable this setting to improve response times.
;                           Disable this setting to play all voicelines natively in-game.
;   fast_response_mode must either be 'True' or 'False'
;   default = False
fast_response_mode = False

; fast_response_mode_volume
;   Adjust the volume of the first delivered voiceline (from 1-100) if Fast Response Mode is enabled.
;   fast_response_mode_volume must be an integer between 1 and 100
;   default = 40
fast_response_mode_volume = 40

; allow_per_character_tts_overrides
;   Allow individual characters to use a different TTS service in conversations.
;               To use this feature, add a 'tts_service' value under the appropriate column in the characters CSV file.
;               Characters without this column will fall back to the global TTS settings.
;   allow_per_character_tts_overrides must either be 'True' or 'False'
;   default = False
allow_per_character_tts_overrides = False

; xtts_url
;   The URL that your XTTS server is running on.
;                           Examples:
;                           http://127.0.0.1:8020 when running XTTS locally.
;                           https://{POD_ID}-8020.proxy.runpod.net when running XTTS in a RunPod GPU pod.
;   default = http://127.0.0.1:8020
xtts_url = http://127.0.0.1:8020

; xtts_default_model
;   Official base XTTS-V2 model to use.
;   Options: v2.0.0, v2.0.1, v2.0.2, v2.0.3, main
;   default = main
xtts_default_model = main

; xtts_device
;   Set to cpu or cuda (NVIDIA GPUs only). You can also specify which NVIDIA GPU to use (cuda:0, cuda:1 etc)
;   Options: cpu, cuda, cuda:0, cuda:1, cuda:2, cuda:3, cuda:4, cuda:5, cuda:6, cuda:7
;   default = cpu
xtts_device = cpu

; xtts_deepspeed
;   Allows you to speed up processing by several times (highly recommended for NVIDIA GPU players!).
;   Only usable with an NVIDIA GPU that supports CUDA 11.8+.
;   xtts_deepspeed must either be 'True' or 'False'
;   default = False
xtts_deepspeed = False

; xtts_lowvram
;   The mode in which the model will be stored in RAM and when the processing occurs it will move to VRAM. The difference in speed is small.
;                       If you don't want to pre-generate the latents for every speaker, enable this setting or else it will generate the latents at every start.
;   xtts_lowvram must either be 'True' or 'False'
;   default = True
xtts_lowvram = True

; xtts_data
;   Default settings passed to the XTTS API server.
;   default = {"temperature": 0.75,
;                       "length_penalty": 1.0,
;                       "repetition_penalty": 5.0,
;                       "top_k": 50,
;                       "top_p": 0.85,
;                       "speed": 1,
;                       "enable_text_splitting": true,
;                       "stream_chunk_size": 100}
xtts_data = "{\"temperature\": 0.75,\n                    \"length_penalty\": 1.0,\n                    \"repetition_penalty\": 5.0,\n                    \"top_k\": 50,\n                    \"top_p\": 0.85,\n                    \"speed\": 1,\n                    \"enable_text_splitting\": true,\n                    \"stream_chunk_size\": 100}"

; xtts_accent
;   Note that this setting is only available for Skyrim.
;           Changes the 'accent' of NPCs by sending the language value from data/Skyrim/skyrim_characters.csv's lang_override column to XTTS.
;   This helps give NPC's unique-sounding voices, even when they use the same base voice model.
;   xtts_accent must either be 'True' or 'False'
;   default = False
xtts_accent = False

; tts_print
;   Print the output from the TTS service in the Mantella.exe window.
;   tts_print must either be 'True' or 'False'
;   default = False
tts_print = False

; tts_process_device
;   Whether to run xVASynth server (unless already running) on your CPU or a NVIDIA GPU (with CUDA installed).
;   This setting has a minimal effect on response times.
;   Options: cpu, gpu
;   default = cpu
tts_process_device = cpu

; pace
;   The default speed of talking. The effect can vary between voices.
;                           0.5 = 2x faster; 2 = 2x slower
;   pace must be a floating point number between 0.1 and 2.0
;   default = 1.0
pace = 1.0

; use_cleanup
;   Whether to try to reduce noise and the robot-sounding nature of xVASynth generated speech.
;                           Has a slight impact on processing speed for the CPU.
;                           Not meant to be used on voices that have post-effects attached to them (echoes, reverbs, etc).
;   use_cleanup must either be 'True' or 'False'
;   default = False
use_cleanup = False

; use_sr
;   Whether to improve the quality of your audio through Super-resolution of 22050Hz audio into 48000Hz audio.
;                           This is a fairly slow process on CPUs, but on some GPUs it can be relatively fast.
;   use_sr must either be 'True' or 'False'
;   default = False
; Enabled: the first voiceline is the one the player is actually waiting on, so
; playing it straight from the Mantella exe skips a round trip through the game.
use_sr = True

[STT]
; Settings for the STT methods Mantella supports.

; audio_threshold
;   Controls how much background noise is filtered out (from 0-1).
;                                           If the mic is not picking up speech, try lowering this value.
;                                           If the mic is picking up too much background noise, try increasing this value.
;   audio_threshold must be a floating point number between 0 and 1
;   default = 0.4
; Set from measured mic captures: failed recordings sat at 0.000-0.010 RMS while
; every successful one was 0.120-0.209. 0.05 sits in that gap - it rejects the
; dead/near-silent captures without ever gating real speech.
audio_threshold = 0.05

; allow_interruption
;   Sets whether the player can interrupt NPCs mid-response.
;                           Disable this setting if your microphone tends to pick up background noise or is picking up in-game speech. Alternatively, try increasing `Audio Threshold`.
;   allow_interruption must either be 'True' or 'False'
;   default = True
allow_interruption = True

; save_mic_input
;   Whether to save captured mic input to Documents/My Games/Mantella/data/tmp/mic/.
;                           Enable this setting to test your mic quality.
;                           Disable this setting to improve performance.
;   save_mic_input must either be 'True' or 'False'
;   default = False
; Was enabled to debug "could not detect speech"; the STT proxy now measures and
; logs every utterance itself, so writing a WAV per line is pure disk overhead.
; Re-enable if you need the raw captures again.
save_mic_input = False

; stt_service
;   Choose between running Moonshine or Whisper as your speech to text service.
;                           Moonshine runs faster than Whisper on a CPU, but only support English.
;                           Whisper can run on a CPU, GPU, or via an external service (see Advanced settings below).
;   Options: Moonshine, Whisper
;   default = Moonshine
stt_service = Whisper

; pause_threshold
;   How long to wait (in seconds) before converting mic input to text.
;                       If you feel like you are being cut off before you finish your response, increase this value.
;                       If you feel like there is too much of a delay between you finishing your response and the text conversion, decrease this value.
;                       Set this value to 0 for faster response times.
;   pause_threshold must be a floating point number between 0 and 999
;   default = 0.25
; Push-to-talk is enabled below, so releasing the key already marks the end of the
; utterance -- waiting a further 0.25s for silence detection added nothing but delay.
; Raise this back to 0.25 if ptt_enabled is ever turned off.
pause_threshold = 0

; ptt_enabled
;   When enabled, speech is only captured while the PTT hotkey is held.
;   ptt_enabled must either be 'True' or 'False'
;   default = False
ptt_enabled = True

; ptt_hotkey
;   Examples: V, SPACE, ENTER, F1..F12.
;   default = V
ptt_hotkey = V

; play_cough_sound
;   If enabled, a generic cough sound will play when the speech-to-text service fails to transcribe player input.
;                           Enable this setting if you are playing in VR / full screen and need an audio cue to know when you haven't been heard.
;                           Disable this setting if its annoying.
;   play_cough_sound must either be 'True' or 'False'
;   default = True
play_cough_sound = True

; listen_timeout
;   How long to wait (in seconds) for the player to speak before retrying.
;                       This needs to be set to ensure that Mantella can periodically check if the conversation has ended.
;   listen_timeout must be an integer between 0 and 999
;   default = 30
listen_timeout = 30

; moonshine_model_size
;   The size of the Moonshine model to use (sorted from smallest to largest). The larger the model, the more accurate the transcription (at the cost of speed).
;   Options: moonshine/tiny/quantized_4bit, moonshine/tiny/quantized, moonshine/tiny/float, moonshine/base/quantized_4bit, moonshine/base/quantized, moonshine/base/float
;   default = moonshine/tiny/quantized
moonshine_model_size = moonshine/tiny/quantized

; whisper_model_size
;   The size of the Whisper model used. Some languages require larger models. The base.en model works well enough for English.
;                           See here for a comparison of languages and their Whisper performance:
;                           https://github.com/openai/whisper#available-models-and-languages
;   
;                           faster-skyrim-whisper-base.en is trained on Skyrim dialogue. It is more accurate in transcribing Skyrim names and locations:
;                           https://github.com/mikastamm/skyrim-whisper-base.en
;   Options: tiny, tiny.en, base, base.en, Numbat/faster-skyrim-whisper-base.en, small, small.en, distil-small.en, medium, medium.en, distil-medium.en, large-v1, large-v2, large-v3, distil-large-v2, distil-large-v3, whisper-1
;   default = base
whisper_model_size = whisper-large-v3-turbo

; proactive_mic_mode
;   If enabled, mic input will be transcribed continuously (every `Refresh Frequency` seconds) as long as speech is detected.
;                           Enable this setting to improve response times.
;                           Disable this setting to improve performance.
;   proactive_mic_mode must either be 'True' or 'False'
;   default = False
proactive_mic_mode = False

; min_refresh_secs
;   Controls how frequently mic input is transcribed by Moonshine / Whisper.
;                           Increase this value if the frequency is too intense for your hardware.
;                           Decrease this value to improve response times.
;   min_refresh_secs must be a floating point number between 0.01 and 999
;   default = 0.3
min_refresh_secs = 0.3

; external_whisper_service
;   Allows running of Whisper externally. When enabled, Mantella will call the URL set in 'Whisper URL' instead of running Whisper locally.
;   external_whisper_service must either be 'True' or 'False'
;   default = False
external_whisper_service = True

; whisper_url
;   The external Whisper service URL Mantella will connect to (if 'External Whisper Service' is enabled).
;                           Some services require an API secret key. This secret key can be set in your `GPT_SECRET_KEY.txt` file. If you don't see the service you would like to connect to in the dropdown list, you can also manually enter a URL to connect to.
;   
;                           Known services:
;   	                        OpenAI: Ensure 'Speech-to-Text'->'Model Size' is set to `whisper-1`. Requires an OpenAI secret key.
;                               Groq: Ensure 'Speech-to-Text'->'Model Size' is set to one of the following: https://console.groq.com/docs/speech-text#supported-models. Requires a Groq secret key.
;                               whisper.cpp: whisper.cpp (https://github.com/ggerganov/whisper.cpp) can be connected to when it is run in server mode. No secret key is required. Ensure the server is running before starting Mantella. By default, selecting whisper.cpp will connect to the URL http://127.0.0.1:8080/inference, but you can also manually enter a URL in this field if you have selected a port other than 8080 or are running whisper.cpp on another machine.
;   Options: OpenAI, Groq, whisper.cpp
;   default = OpenAI
; Pointed at Sentient's own STT proxy instead of straight at Groq. Sentient still
; forwards the audio to Groq using the key from GPT_SECRET_KEY.txt, but it measures
; and prints every utterance (duration, mic level, transcription) on the way past,
; which is what makes "Could not detect speech" debuggable.
; To go back to talking to Groq directly, set this to: Groq
whisper_url = http://localhost:8000/v1/audio/transcriptions

; stt_language
;   The player's spoken language.
;   Options: default, en, ar, cs, da, de, el, es, fi, fr, hi, hu, it, ja, ko, nl, pl, pt, ro, ru, sv, sw, uk, ha, tr, vi, yo
;   default = default
; Pinned to 'en' rather than left on auto-detect: Whisper's language detection is
; unreliable on the short utterances push-to-talk produces and silently returns
; empty or garbled text when it guesses wrong.
stt_language = en

; stt_translate
;   Translate the transcribed speech to English if supported by the Speech-To-Text engine (only impacts faster_whisper option, no impact on whispercpp, which is controlled by your server).
;                           STTs that support this function: Whisper (faster_whisper).
;   stt_translate must either be 'True' or 'False'
;   default = False
stt_translate = False

; process_device
;   Whether to run Whisper on your CPU or NVIDIA GPU (with CUDA installed) (only impacts faster_whisper option, no impact on whispercpp, which is controlled by your server).
;   Options: cpu, cuda
;   default = cpu
process_device = cpu

; moonshine_folder
;   The folder where Moonshine models are installed (where tiny/ and base/ folders exist).
moonshine_folder =

; silence_auto_response_enabled
;   If enabled, the AI will automatically generate a response if the player is silent for a period of time.
;   silence_auto_response_enabled must either be 'True' or 'False'
;   default = False
silence_auto_response_enabled = False

; silence_auto_response_timeout
;   How long to wait (in seconds) for the player to speak before automatically generating a response.
;   silence_auto_response_timeout must be a floating point number between 1 and 999
;   default = 30.0
silence_auto_response_timeout = 30.0

; silence_auto_response_max_count
;   The maximum number of consecutive auto-responses before auto-response is disabled (until the player speaks again).
;                           This is mainly to prevent runaway API costs if the player leaves the conversation running unattended.
;   silence_auto_response_max_count must be an integer between 1 and 999
;   default = 5
silence_auto_response_max_count = 5

; silence_auto_response_message
;   The message sent to the AI when the player is silent.
;   default = ...
silence_auto_response_message = ...

[Vision]
; Vision settings.

; vision_enabled
;   If enabled, in-game screenshots are passed to the chosen LLM with each player response.
;                           This feature is only compatible with LLMs that accept image as well as text input, unless `Custom Vision Model` is enabled.
;                           Please ensure your game window is visible on screen when running and is not blocked by other windows!
;   vision_enabled must either be 'True' or 'False'
;   default = False
vision_enabled = False

; low_resolution_mode
;   Resizes the image to 512x512 pixels (cropping the edges of the longest side). Enable this setting to lower API costs and improve performance.
;   low_resolution_mode must either be 'True' or 'False'
;   default = True
low_resolution_mode = True

; save_screenshot
;   Whether to save screenshots to Documents/My Games/Mantella/data/tmp/images/. Disable this setting to improve performance.
;   save_screenshot must either be 'True' or 'False'
;   default = True
save_screenshot = True

; image_quality
;   The quality of the image passed to the LLM from 1-100. Higher values improve the LLM's understanding of passed images. Lower values slightly improve performance. This setting has no affect on API costs.
;   image_quality must be an integer between 1 and 100
;   default = 50
image_quality = 50

; resize_method
;   The image scaling algorithm used to resize in-game screenshots to match the target resolution. Algorithms are sorted from fastest / lowest quality (Nearest) to slowest / highest quality (Lanczos).
;   Options: Nearest, Linear, Cubic, Lanczos
;   default = Nearest
resize_method = Nearest

; capture_offset
;   The number of pixels to offset the capture window. Adjust these numbers with either positive or negative values if the game window is not being captured in the correct dimensions.
;   default = {"left": 0, "right": 0, "top": 0, "bottom": 0}
capture_offset = {"left": 0, "right": 0, "top": 0, "bottom": 0}

; custom_vision_model
;   Whether to call a separate vision-capable LLM for image handling (NPCs will still respond using the model chosen in the `Large Language Model` tab).
;                           If enabled, please configure the vision-capable LLM to connect to below.
;                           Note that calling a separate LLM for image handling will increase response times.
;   custom_vision_model must either be 'True' or 'False'
;   default = False
custom_vision_model = False

; vision_llm_api
;   Selects the vision model service to connect to (either local or via an API).
;   
;               If you are connecting to a local service (KoboldCpp, textgenwebui etc), please ensure that the service is running and a model is loaded. You can also enter a custom URL to connect to other LLM services that provide an OpenAI compatible endpoint.
;               After selecting a service, select the model using the option below. Press the *Update* button to load a list of models available from the service.
;   
;               Some services require an API secret key. This secret key can be set in your `GPT_SECRET_KEY.txt` file.
;   Options: OpenRouter, OpenAI, NanoGPT, KoboldCpp, textgenwebui
;   default = OpenRouter
vision_llm_api = OpenRouter

; vision_model
;   Select the vision model to use. Press the *Update* button to load a list of models available from the service selected above.
;                               The list does not provide all details about the models. For additional information please refer to the corresponding sites:
;                               - OpenRouter: https://openrouter.ai/models?modality=text%2Bimage-%3Etext
;                               - OpenAI: https://platform.openai.com/docs/models https://openai.com/api/pricing/
;   Options: Custom Model
;   default = google/gemma-3-27b-it:free
vision_model = google/gemma-3-27b-it:free

; vision_custom_token_count
;   If the vision model chosen is not recognised by Mantella, the token count for the given model will default to this number.
;                       If this is not the correct token count for your chosen model, you can change it here.
;                       Keep in mind that if this number is greater than the actual token count of the model, then Mantella will crash if a given conversation exceeds the model's token limit.
;   vision_custom_token_count must be an integer between 4096 and 9999999
;   default = 4096
vision_custom_token_count = 4096

; vision_llm_params
;   Parameters passed as part of the request to the vision model.
;                           A list of the most common parameters can be found here: https://openrouter.ai/docs/parameters.
;                           Note that available parameters can vary per LLM provider.
;   default = {
;                           "max_tokens": 100,
;                           "stop": ["#"]
;                       }
vision_llm_params = "{\n                        \"max_tokens\": 100,\n                        \"stop\": [\"\#\"]\n                    }"

; use_game_screenshots
;   Whether to use screenshots taken in-game instead of relying on Mantella to capture the game window (Fallout 4 only).
;                           Enable this setting to improve screenshot capture reliability. Otherwise please ensure your game window is visible on screen when running and is not blocked by other windows.
;                           If enabled, please ensure you configure screenshot capture settings in the Mantella Pip-Boy settings window.
;   use_game_screenshots must either be 'True' or 'False'
;   default = False
use_game_screenshots = False

[Actions]
; Settings for in-game actions.

; advanced_actions_enabled
;   If enabled, LLMs with tool calling capabilities can trigger advanced in-game actions.
;                           If disabled, only basic actions can be triggered by the LLM using an '[action_name]: [NPC response]' format (eg 'Follow: Lead the way').
;                           Actions without a 'prompt' field defined in the action's JSON file (see data/actions/) can only be triggered when this setting is enabled.
;   advanced_actions_enabled must either be 'True' or 'False'
;   default = False
advanced_actions_enabled = False

; disabled_actions
;   Actions not selected here will be available in conversations.
;                           Some actions can only be triggered when `Advanced Actions` is enabled above.
;                           To learn more about an action, see the associated JSON file in the data/actions/ folder.
;   Options: AbsolveCrime, AddToConversation, Attack, Barter, Brawl, CancelTravel, CastSpell, CheckDirections, CollectIngredients, Emote, EndConversation, Flee, Follow, Inventory, LeadTo, Listen, Look, Loot, MoveTo, ReportCrime, ShareConversation, StandDown, Teleport, TravelTo, Unfollow, Wait
;   default = AbsolveCrime, AddToConversation, Brawl, CancelTravel, CastSpell, CheckDirections, CollectIngredients, Emote, Follow, LeadTo, Look, Loot, ReportCrime, ShareConversation, Teleport, TravelTo, Unfollow, Wait
disabled_actions = AbsolveCrime, AddToConversation, Brawl, CancelTravel, CastSpell, CheckDirections, CollectIngredients, Emote, Follow, LeadTo, Look, Loot, ReportCrime, ShareConversation, Teleport, TravelTo, Unfollow, Wait

; custom_function_model
;   Whether to call a separate tool-calling-capable LLM for advanced action handling (NPC verbal responses will still use the model chosen in the `Large Language Model` tab).
;                           If enabled, please configure the tool-calling-capable LLM to connect to below.
;                           If disabled, the model chosen in the `Large Language Model` tab will be used for both verbal responses and advanced action handling (if advanced actions are enabled).
;   custom_function_model must either be 'True' or 'False'
;   default = False
custom_function_model = False

; function_llm_api
;   Selects the LLM service to connect to (either local or via an API) that will handle NPC actions.
;               Please ensure the selected model supports tool calling.
;               If you are connecting to a local service (KoboldCpp, textgenwebui etc), please ensure that the service is running and a model is loaded. You can also enter a custom URL to connect to other LLM services that provide an OpenAI compatible endpoint.
;               After selecting a service, select the model using the option below. Press the *Update list* button to load a list of models available from the service.
;   
;               **If you are using an API (OpenAI, OpenRouter, etc) ensure you have the correct secret key set in `GPT_SECRET_KEY.txt` for the respective service you are using.
;   Options: OpenRouter, OpenAI, NanoGPT, KoboldCpp, textgenwebui
;   default = OpenRouter
function_llm_api = OpenRouter

; function_llm
;   Select the tool calling model to handle NPC actions. Press the *Update* button to load a list of models available from the service selected above.
;                               The list does not provide all details about the models. For additional information please refer to the corresponding sites:
;                               - OpenRouter: https://openrouter.ai/models?fmt=cards&supported_parameters=tools
;                               - OpenAI: https://platform.openai.com/docs/models https://openai.com/api/pricing/
;   Options: Custom Model
;   default = google/gemma-4-26b-a4b-it:free
function_llm = google/gemma-4-26b-a4b-it:free

; function_llm_custom_token_count
;   If the model chosen is not recognized by Mantella, the token count for the given model will default to this number.
;                       If this is not the correct token count for your chosen model, you can change it here.
;                       Keep in mind that if this number is greater than the actual token count of the model, then Mantella will crash if a given conversation exceeds the model's token limit.
;   function_llm_custom_token_count must be an integer between 4096 and 9999999
;   default = 4096
function_llm_custom_token_count = 4096

; function_llm_params
;   Parameters passed as part of the request to the tool calling model.
;                           The "tools" parameter is automatically added by Mantella and should not be included here.
;                           A list of the most common parameters can be found here: https://openrouter.ai/docs/parameters.
;                           Note that available parameters can vary per LLM provider.
;   default = {
;                           "max_tokens": 500,
;                           "stop": ["#"]
;                       }
function_llm_params = "{\n                        \"max_tokens\": 500,\n                        \"stop\": [\"\#\"]\n                    }"

[Language]
; Change the language used by Mantella as well as keywords.

; language
;   The language used by Mantella (speech-to-text, LLM responses, and text-to-speech).
;   Options: en, ar, cs, da, de, el, es, fi, fr, hi, hu, it, ja, ko, nl, pl, pt, ro, ru, sv, sw, uk, ha, tr, vi, yo, zh
;   default = en
language = en

; end_conversation_keyword
;   The keyword(s) Mantella will listen out for to end the conversation (lowercase / uppercase does not matter).
;                           To add multiple options, you can split keywords using commas.
;   default = goodbye, bye, good-bye, good bye, good-by, good by, good to buy
end_conversation_keyword = goodbye, bye, good-bye, good bye, good-by, good by, good to buy

; goodbye_npc_response
;   The response the NPC gives at the end of the conversation.
;   default = Safe travels
goodbye_npc_response = Safe travels

; collecting_thoughts_npc_response
;   The response the NPC gives when they need to summarise the conversation because the maximum token count has been reached.
;   default = I need to gather my thoughts for a moment
collecting_thoughts_npc_response = I need to gather my thoughts for a moment

[Prompts]
; Change the basic prompts used by Mantella.

; skyrim_prompt
;   The starting prompt sent to the LLM when an NPC is selected.
;                                   The following are dynamic variables that need to be contained in curly brackets {}:
;                                   name = the NPC's name
;                                   names = the names of all NPCs in the conversation
;                                   names_w_player = the names of all NPCs in the conversation and the name of the player character
;                                   game = the selected game
;                                   bio = the NPC's background description
;                                   trust = how well the NPC knows the player (eg "a stranger", "a friend")
;                                   location = the current location
;                                   weather = the current weather
;                                   time = the time of day as a number (eg 1, 22)
;                                   time_group = the time of day in words (eg "in the morning", "at night")
;                                   language = the selected language
;                                   conversation_summary = reads the latest conversation summaries for the NPC stored in data/conversations/NPC_Name/NPC_Name_summary_X.txt
;                                   player_name = the name of the player character
;                                   player_description = a description of the player character (needs to be added in game or using the config value)
;                                   player_equipment = a basic description of the equipment the player character carries
;                                   equipment = a basic description of the equipment the NPCs carry
;                                   actions = instructions for the LLM how to trigger actions (if advanced actions are disabled, otherwise action instructions are already passed to the LLM as tools via the 'description' field)
;   default = # Overview
;   You are {name} in Skyrim. You are talking with {player_name} (the player). The player {player_equipment} {equipment}
;   
;   # Background
;   {bio}
;   
;   # History
;   {conversation_summary}
;   
;   # Current Scene
;   You are now in {location}. The time is {time} {time_group} on Day {current_day}. {weather}
;   
;   # Rules
;   {actions}
;   Use tools to perform actions where appropriate.
;   Output ONLY spoken dialogue. No narration, no descriptions, no thoughts.
;   Do not use quotation marks.
;   Stay in character.
;   
;   Respond as {name} now in {language}.
skyrim_prompt = "\# Overview\nYou are {name} in Skyrim. You are talking with {player_name} (the player). The player {player_equipment} {equipment}\n\n\# Background\n{bio}\n\n\# History\n{conversation_summary}\n\n\# Current Scene\nYou are now in {location}. The time is {time} {time_group} on Day {current_day}. {weather}\n\n\# Rules\n{actions}\nUse tools to perform actions where appropriate.\nOutput ONLY spoken dialogue. No narration, no descriptions, no thoughts.\nDo not use quotation marks.\nStay in character.\n\nRespond as {name} now in {language}."

; skyrim_multi_npc_prompt
;   The starting prompt sent to the LLM when an NPC is selected.
;                                   The following are dynamic variables that need to be contained in curly brackets {}:
;                                   name = the NPC's name
;                                   names = the names of all NPCs in the conversation
;                                   names_w_player = the names of all NPCs in the conversation and the name of the player character
;                                   game = the selected game
;                                   bio = the NPC's background description
;                                   trust = how well the NPC knows the player (eg "a stranger", "a friend")
;                                   location = the current location
;                                   weather = the current weather
;                                   time = the time of day as a number (eg 1, 22)
;                                   time_group = the time of day in words (eg "in the morning", "at night")
;                                   language = the selected language
;                                   conversation_summary = reads the latest conversation summaries for the NPC stored in data/conversations/NPC_Name/NPC_Name_summary_X.txt
;                                   player_name = the name of the player character
;                                   player_description = a description of the player character (needs to be added in game or using the config value)
;                                   player_equipment = a basic description of the equipment the player character carries
;                                   equipment = a basic description of the equipment the NPCs carry
;                                   actions = instructions for the LLM how to trigger actions (if advanced actions are disabled, otherwise action instructions are already passed to the LLM as tools via the 'description' field)
;   default = # Overview
;   The following is a conversation in Skyrim between {names_w_player} (the player). The player {player_equipment} {equipment}
;   
;   # Background
;   {bios}
;   
;   # History
;   {conversation_summaries}
;   
;   # Current Scene
;   The location is now {location}. The time is {time} {time_group} on Day {current_day}. {weather}
;   
;   # Rules
;   {actions}
;   Use tools to perform actions where appropriate.
;   You are tasked with providing the responses for the NPCs. Begin your response with an indication of who you are speaking as, for example: '{name}: Good evening.'.
;   Use your own discretion to decide who should speak in a given situation (sometimes responding with all NPCs is suitable).
;   Output ONLY spoken dialogue. No narration, no descriptions, no thoughts.
;   Do not use quotation marks.
;   Stay in character.
;   
;   Remember, you can only respond as {names}. Ensure to use their full name when responding. Respond now in {language}.
skyrim_multi_npc_prompt = "\# Overview\nThe following is a conversation in Skyrim between {names_w_player} (the player). The player {player_equipment} {equipment}\n\n\# Background\n{bios}\n\n\# History\n{conversation_summaries}\n\n\# Current Scene\nThe location is now {location}. The time is {time} {time_group} on Day {current_day}. {weather}\n\n\# Rules\n{actions}\nUse tools to perform actions where appropriate.\nYou are tasked with providing the responses for the NPCs. Begin your response with an indication of who you are speaking as, for example: '{name}: Good evening.'.\nUse your own discretion to decide who should speak in a given situation (sometimes responding with all NPCs is suitable).\nOutput ONLY spoken dialogue. No narration, no descriptions, no thoughts.\nDo not use quotation marks.\nStay in character.\n\nRemember, you can only respond as {names}. Ensure to use their full name when responding. Respond now in {language}."

; skyrim_radiant_prompt
;   The starting prompt sent to the LLM when a radiant conversation is started.
;                                   The following are dynamic variables that need to be contained in curly brackets {}:
;                                   name = the NPC's name
;                                   names = the names of all NPCs in the conversation
;                                   game = the selected game
;                                   bio = the backgrounds of the NPCs
;                                   location = the current location
;                                   weather = the current weather
;                                   time = the time of day as a number (eg 1, 22)
;                                   time_group = the time of day in words (eg "in the morning", "at night")
;                                   language = the selected language
;                                   conversation_summary = reads the latest conversation summaries for the NPCs stored in data/conversations/NPC_Name/NPC_Name_summary_X.txt
;                                   equipment = a basic description of the equipment the NPCs carry
;                                   actions = instructions for the LLM to trigger actions (if advanced actions are disabled, otherwise action instructions are already passed to the LLM as tools via the 'description' field)
;   default = # Overview
;   The following is a conversation in Skyrim between {names}. {equipment}
;   
;   # Background
;   {bios}
;   
;   # History
;   {conversation_summaries}
;   
;   # Current Scene
;   The location is now {location}. The time is {time} {time_group} on Day {current_day}. {weather}
;   
;   # Rules
;   {actions}
;   Use tools to perform actions where appropriate.
;   You are tasked with providing the responses for the NPCs. Begin your response with an indication of who you are speaking as, for example: '{name}: Good evening.'.
;   Multiple NPCs should speak each turn.
;   Output ONLY spoken dialogue. No narration, no descriptions, no thoughts.
;   Do not use quotation marks.
;   Stay in character.
;   
;   Remember, you can only respond as {names}. Ensure to use their full name when responding. Respond now in {language}.
skyrim_radiant_prompt = "\# Overview\nThe following is a conversation in Skyrim between {names}. {equipment}\n\n\# Background\n{bios}\n\n\# History\n{conversation_summaries}\n\n\# Current Scene\nThe location is now {location}. The time is {time} {time_group} on Day {current_day}. {weather}\n\n\# Rules\n{actions}\nUse tools to perform actions where appropriate.\nYou are tasked with providing the responses for the NPCs. Begin your response with an indication of who you are speaking as, for example: '{name}: Good evening.'.\nMultiple NPCs should speak each turn.\nOutput ONLY spoken dialogue. No narration, no descriptions, no thoughts.\nDo not use quotation marks.\nStay in character.\n\nRemember, you can only respond as {names}. Ensure to use their full name when responding. Respond now in {language}."

; fallout4_prompt
;   The starting prompt sent to the LLM when an NPC is selected.
;                                   The following are dynamic variables that need to be contained in curly brackets {}:
;                                   name = the NPC's name
;                                   names = the names of all NPCs in the conversation
;                                   names_w_player = the names of all NPCs in the conversation and the name of the player character
;                                   game = the selected game
;                                   bio = the NPC's background description
;                                   trust = how well the NPC knows the player (eg "a stranger", "a friend")
;                                   location = the current location
;                                   weather = the current weather
;                                   time = the time of day as a number (eg 1, 22)
;                                   time_group = the time of day in words (eg "in the morning", "at night")
;                                   language = the selected language
;                                   conversation_summary = reads the latest conversation summaries for the NPC stored in data/conversations/NPC_Name/NPC_Name_summary_X.txt
;                                   player_name = the name of the player character
;                                   player_description = a description of the player character (needs to be added in game or using the config value)
;                                   player_equipment = a basic description of the equipment the player character carries
;                                   equipment = a basic description of the equipment the NPCs carry
;                                   actions = instructions for the LLM how to trigger actions (if advanced actions are disabled, otherwise action instructions are already passed to the LLM as tools via the 'description' field)
;   default = # Overview
;   You are {name} in the post-apocalyptic Commonwealth of Fallout. You are talking with the player.
;   
;   # Background
;   {bio}
;   
;   # History
;   {conversation_summary}
;   
;   # Current Scene
;   You are now in {location}. The time is {time} {time_group}.
;   
;   # Rules
;   {actions}
;   Use tools to perform actions where appropriate.
;   Output ONLY spoken dialogue. No narration, no descriptions, no thoughts.
;   Do not use quotation marks.
;   Stay in character.
;   
;   Respond as {name} now in {language}.
fallout4_prompt = "\# Overview\nYou are {name} in the post-apocalyptic Commonwealth of Fallout. You are talking with the player.\n\n\# Background\n{bio}\n\n\# History\n{conversation_summary}\n\n\# Current Scene\nYou are now in {location}. The time is {time} {time_group}.\n\n\# Rules\n{actions}\nUse tools to perform actions where appropriate.\nOutput ONLY spoken dialogue. No narration, no descriptions, no thoughts.\nDo not use quotation marks.\nStay in character.\n\nRespond as {name} now in {language}."

; fallout4_multi_npc_prompt
;   The starting prompt sent to the LLM when an NPC is selected.
;                                   The following are dynamic variables that need to be contained in curly brackets {}:
;                                   name = the NPC's name
;                                   names = the names of all NPCs in the conversation
;                                   names_w_player = the names of all NPCs in the conversation and the name of the player character
;                                   game = the selected game
;                                   bio = the NPC's background description
;                                   trust = how well the NPC knows the player (eg "a stranger", "a friend")
;                                   location = the current location
;                                   weather = the current weather
;                                   time = the time of day as a number (eg 1, 22)
;                                   time_group = the time of day in words (eg "in the morning", "at night")
;                                   language = the selected language
;                                   conversation_summary = reads the latest conversation summaries for the NPC stored in data/conversations/NPC_Name/NPC_Name_summary_X.txt
;                                   player_name = the name of the player character
;                                   player_description = a description of the player character (needs to be added in game or using the config value)
;                                   player_equipment = a basic description of the equipment the player character carries
;                                   equipment = a basic description of the equipment the NPCs carry
;                                   actions = instructions for the LLM how to trigger actions (if advanced actions are disabled, otherwise action instructions are already passed to the LLM as tools via the 'description' field)
;   default = # Overview
;   The following is a conversation in the post-apocalyptic Commonwealth of Fallout between {names_w_player}.
;   
;   # Background
;   {bios}
;   
;   # History
;   {conversation_summaries}
;   
;   # Current Scene
;   The location is now {location}. The time is {time} {time_group}.
;   
;   # Rules
;   {actions}
;   Use tools to perform actions where appropriate.
;   You are tasked with providing the responses for the NPCs. Begin your response with an indication of who you are speaking as, for example: '{name}: Good evening.'.
;   Use your own discretion to decide who should speak in a given situation (sometimes responding with all NPCs is suitable).
;   Output ONLY spoken dialogue. No narration, no descriptions, no thoughts.
;   Do not use quotation marks.
;   Stay in character.
;   
;   Remember, you can only respond as {names}. Ensure to use their full name when responding. Respond now in {language}.
fallout4_multi_npc_prompt = "\# Overview\nThe following is a conversation in the post-apocalyptic Commonwealth of Fallout between {names_w_player}.\n\n\# Background\n{bios}\n\n\# History\n{conversation_summaries}\n\n\# Current Scene\nThe location is now {location}. The time is {time} {time_group}.\n\n\# Rules\n{actions}\nUse tools to perform actions where appropriate.\nYou are tasked with providing the responses for the NPCs. Begin your response with an indication of who you are speaking as, for example: '{name}: Good evening.'.\nUse your own discretion to decide who should speak in a given situation (sometimes responding with all NPCs is suitable).\nOutput ONLY spoken dialogue. No narration, no descriptions, no thoughts.\nDo not use quotation marks.\nStay in character.\n\nRemember, you can only respond as {names}. Ensure to use their full name when responding. Respond now in {language}."

; fallout4_radiant_prompt
;   The starting prompt sent to the LLM when a radiant conversation is started.
;                                   The following are dynamic variables that need to be contained in curly brackets {}:
;                                   name = the NPC's name
;                                   names = the names of all NPCs in the conversation
;                                   game = the selected game
;                                   bio = the backgrounds of the NPCs
;                                   location = the current location
;                                   weather = the current weather
;                                   time = the time of day as a number (eg 1, 22)
;                                   time_group = the time of day in words (eg "in the morning", "at night")
;                                   language = the selected language
;                                   conversation_summary = reads the latest conversation summaries for the NPCs stored in data/conversations/NPC_Name/NPC_Name_summary_X.txt
;                                   equipment = a basic description of the equipment the NPCs carry
;                                   actions = instructions for the LLM to trigger actions (if advanced actions are disabled, otherwise action instructions are already passed to the LLM as tools via the 'description' field)
;   default = # Overview
;   The following is a conversation in the post-apocalyptic Commonwealth of Fallout between {names}.
;   
;   # Background
;   {bios}
;   
;   # History
;   {conversation_summaries}
;   
;   # Current Scene
;   The location is now {location}. The time is {time} {time_group}.
;   
;   # Rules
;   {actions}
;   Use tools to perform actions where appropriate.
;   You are tasked with providing the responses for the NPCs. Begin your response with an indication of who you are speaking as, for example: '{name}: Good evening.'.
;   Multiple NPCs should speak each turn.
;   Output ONLY spoken dialogue. No narration, no descriptions, no thoughts.
;   Do not use quotation marks.
;   Stay in character.
;   
;   Remember, you can only respond as {names}. Ensure to use their full name when responding. Respond now in {language}.
fallout4_radiant_prompt = "\# Overview\nThe following is a conversation in the post-apocalyptic Commonwealth of Fallout between {names}.\n\n\# Background\n{bios}\n\n\# History\n{conversation_summaries}\n\n\# Current Scene\nThe location is now {location}. The time is {time} {time_group}.\n\n\# Rules\n{actions}\nUse tools to perform actions where appropriate.\nYou are tasked with providing the responses for the NPCs. Begin your response with an indication of who you are speaking as, for example: '{name}: Good evening.'.\nMultiple NPCs should speak each turn.\nOutput ONLY spoken dialogue. No narration, no descriptions, no thoughts.\nDo not use quotation marks.\nStay in character.\n\nRemember, you can only respond as {names}. Ensure to use their full name when responding. Respond now in {language}."

; memory_prompt
;   The prompt used to summarize a conversation and save to the NPC's memories in data/game/conversations/NPC_Name/NPC_Name_summary_X.txt.
;                                            	If you would like to edit this, please ensure that the below dynamic variables are contained in curly brackets {}:
;                                                  name = the NPC name(s)
;                                                  language = the selected language
;                                                  game = the game selected
;                                                  bios = the NPC bio(s)
;                                                  conversation_summaries = summaries of previous conversations
;                                                  player_name = the player's name
;   default = You are tasked with summarizing the conversation between {name} and {player_name} (and any other characters present). These conversations take place in {game}.
;   
;   Here are the character bios for context:
;   {bios}
;   
;   Here are the previous conversation summaries for context:
;   {conversation_summaries}
;   
;   It is not necessary to comment on any mixups in communication such as mishearings. Text contained within brackets state in-game events.
;   Please summarize the conversation into a single paragraph in {language}.
memory_prompt = "You are tasked with summarizing the conversation between {name} and {player_name} (and any other characters present). These conversations take place in {game}.\n\nHere are the character bios for context:\n{bios}\n\nHere are the previous conversation summaries for context:\n{conversation_summaries}\n\nIt is not necessary to comment on any mixups in communication such as mishearings. Text contained within brackets state in-game events.\nPlease summarize the conversation into a single paragraph in {language}."

; memory_prompt_datetime_prefix
;   Whether to prepend a timestamp of when the conversation took place (eg "[Day 42, 5 in the early evening]") to the summary.
;   memory_prompt_datetime_prefix must either be 'True' or 'False'
;   default = True
memory_prompt_datetime_prefix = True

; resummarize_prompt
;   Memories build up over time in data/game/conversations/NPC_Name/NPC_Name_summary_X.txt.
;                                               When these memories become too long to fit into the chosen LLM's maximum context length, these memories need to be condensed down.
;                                               This prompt is used to ask the LLM to summarize an NPC's memories into a single paragraph, and starts a new memory file in data/game/conversations/NPC_Name/NPC_Name_summary_X+1.txt.
;                                               If you would like to edit this, please ensure that the below dynamic variables are contained in curly brackets {}:
;                                                   name = the NPC's name
;                                                   language = the selected language
;                                                   game = the game selected
;                                                   player_name = the player's name
;   default = You are tasked with summarizing the conversation history between {name} and {player_name} (the player) / other characters. These conversations take place in {game}.
;                                               Each paragraph represents a conversation at a new point in time. Timestamps in square brackets (eg [Day 42, 5 in the early evening]) indicate when each conversation occurred. Please summarize these conversations into a single paragraph in {language}.
resummarize_prompt = "You are tasked with summarizing the conversation history between {name} and {player_name} (the player) / other characters. These conversations take place in {game}.\n                                            Each paragraph represents a conversation at a new point in time. Timestamps in square brackets (eg [Day 42, 5 in the early evening]) indicate when each conversation occurred. Please summarize these conversations into a single paragraph in {language}."

; vision_prompt
;   The prompt passed to the vision-capable LLM when `Custom Vision Model` is enabled.
;   default = This image is to give context and is from the player's point of view in the game of {game}.
;                               Describe the details visible inside it without mentioning the game. Refer to it as a scene instead of an image.
vision_prompt = "This image is to give context and is from the player's point of view in the game of {game}.\n                            Describe the details visible inside it without mentioning the game. Refer to it as a scene instead of an image."

; function_llm_prompt
;   The prompt sent to the separate function calling LLM when `Custom Function Model` is enabled.
;                               This LLM analyzes the recent conversation and determines which in-game actions (if any) should be triggered.
;                               The following are dynamic variables that need to be contained in curly brackets {}:
;                               game = the selected game
;   default = You are analyzing a conversation in {game} to determine if any in-game actions should be triggered.
;                                   Based on the recent dialogue, call the appropriate functions to execute actions.
;                                   If no actions are needed, do not call any functions.
function_llm_prompt = "You are analyzing a conversation in {game} to determine if any in-game actions should be triggered.\n                                Based on the recent dialogue, call the appropriate functions to execute actions.\n                                If no actions are needed, do not call any functions."

; radiant_start_prompt
;   Once a radiant conversation has started and the radiant prompt has been passed to the LLM, the below text is passed in replace of the player response.
;                                           This prompt is used to steer the radiant conversation.
;   default = Please begin / continue a conversation topic (greetings are not needed). Ensure to change the topic if the current one is losing steam.
;                               The conversation should steer towards topics which reveal information about the characters and who they are, or instead drive forward previous conversations in their memory.
radiant_start_prompt = "Please begin / continue a conversation topic (greetings are not needed). Ensure to change the topic if the current one is losing steam.\n                            The conversation should steer towards topics which reveal information about the characters and who they are, or instead drive forward previous conversations in their memory."

; radiant_continue_prompt
;   The prompt sent to the LLM between turns in a radiant conversation to keep the conversation going.
;                                               This is used for intermediate turns when Radiant Max Turns is greater than 2.
;   default = Continue the conversation, or call the EndConversation action if the conversation has naturally concluded.
radiant_continue_prompt = Continue the conversation, or call the EndConversation action if the conversation has naturally concluded.

; radiant_end_prompt
;   The final prompt sent to the LLM before ending a radiant conversation.
;                                               This prompt is used to guide the LLM to end the conversation naturally.
;   default = Please wrap up the current topic between the NPCs in a natural way. Nobody is leaving, so there is no need for formal goodbyes.
radiant_end_prompt = Please wrap up the current topic between the NPCs in a natural way. Nobody is leaving, so there is no need for formal goodbyes.

; radiant_max_turns
;   The maximum number of LLM response turns in a radiant conversation.
;                                               Higher values allow for longer conversations.
;                                               If the EndConversation action is enabled for radiant conversation,
;                                               the LLM may choose to end the conversation earlier if it naturally concludes.
;   radiant_max_turns must be an integer between 2 and 999
;   default = 2
radiant_max_turns = 2

[Startup]
; Startup settings.

; auto_launch_ui
;   Whether the Mantella UI should launch automatically in your browser.
;   auto_launch_ui must either be 'True' or 'False'
;   default = True
auto_launch_ui = True

; play_startup_sound
;   Whether to play a startup sound to let you know when Mantella is ready.
;   play_startup_sound must either be 'True' or 'False'
;   default = True
play_startup_sound = True

; remove_mei_folders
;   Clean up older instances of Mantella runtime folders from /data/tmp/_MEIxxxxxx.
;                                               These folders build up over time when Mantella.exe is run.
;                                               Enable this option to clean up these previous folders automatically when Mantella.exe is run.
;                                               Disable this option if running this cleanup inteferes with other Python exes.
;                                               For more details on what this is, see here: https://github.com/pyinstaller/pyinstaller/issues/2379
;   remove_mei_folders must either be 'True' or 'False'
;   default = True
remove_mei_folders = True

[Profiles]
; Profile settings.

; apply_model_profiles
;   When enabled, Mantella will use saved model profiles to override manual LLM parameters.
;                           Model profiles store per-model parameters (temperature, max_tokens, etc) and are applied automatically when a matching profile exists.
;   apply_model_profiles must either be 'True' or 'False'
;   default = False
apply_model_profiles = False

; profile_selected_service
;   Select the LLM service for the profile you want to create or edit.
;                           After selecting a service, choose a model from the available models list below.
;   Options: OpenRouter, OpenAI, NanoGPT, KoboldCpp, textgenwebui
;   default = OpenRouter
profile_selected_service = OpenRouter

; profile_selected_model
;   Select or type the model name for the profile.
;                           Press the *Update* button to load a list of available models from the service selected above.
;   Options: Custom Model
;   default = google/gemma-4-26b-a4b-it:free
profile_selected_model = google/gemma-4-26b-a4b-it:free

; profile_parameters
;   Parameters passed as part of the request to the model selected above.
;                           A list of the most common parameters can be found here: https://openrouter.ai/docs/parameters.
;                           Note that available parameters can vary per LLM provider.
;   default = {
;                           "max_tokens": 250,
;                           "stop": ["#"]
;                       }
profile_parameters = "{\n                        \"max_tokens\": 250,\n                        \"stop\": [\"\#\"]\n                    }"

[Other]
; Other settings.

; automatic_greeting
;   Should a conversation be started with an automatic greeting from the LLM / NPC.
;                                           - If enabled: Conversations are always started by the LLM.
;                                           - If disabled: The LLM will not respond until the player speaks first.
;   automatic_greeting must either be 'True' or 'False'
;   default = True
automatic_greeting = True

; max_count_events
;   Maximum number of in-game events that are sent to the LLM per player message.
;                                       If the maximum number is reached, the oldest events will be dropped.
;                                       Increasing this number will cost more prompt tokens and lead to the context limit being reached faster.
;   max_count_events must be an integer between 0 and 999999
;   default = 5
max_count_events = 5

; events_refresh_time
;   Determines how much time (in seconds) can pass between the last NPC's response and the player's input before in-game events need to be refreshed.
;                                           Note that updating in-game events increases response times. If the player responds before this set number in seconds, response times will be reduced.
;                                           Increase this value to allow more time for the player to respond before events need to be refreshed. Decrease this value to make in-game events more up to date.
;   events_refresh_time must be an integer between 0 and 999999
;   default = 10
events_refresh_time = 10

; hourly_time
;   If enabled, NPCs will be made aware of the time every in-game hour. Otherwise, time updates will be less granular (eg 'The conversation now takes place in the morning' / 'at night' etc).
;                           To remove mentions of the hour entirely, prompts also need to be edited from 'The time is {time} {time_group}.' to 'The conversation takes place {time_group}.'
;   hourly_time must either be 'True' or 'False'
;   default = False
hourly_time = False

; player_character_description
;   A description of your player character in-game. This is sent to the LLM as part of the prompt using the '{player_description}' variable.
;                                                       This is not meant to be a bio but rather a description how the NPC(s) perceive the player character when they speak to them.
;                                                       e.g. 'A tall man with long red hair.'
;                                                       If the in-game MCM offers to set this option the text sent from the game takes precendence over this.
player_character_description =

; voice_player_input
;   Should the input of the player (both by text or voice) be spoken by the player character in-game?
;                                               Can be used for immersion or to fill the initial gap between input and reply.
;                                               Use the 'Player Voice Model' setting to select the voice model of the TTS for the player character.
;   voice_player_input must either be 'True' or 'False'
;   default = False
voice_player_input = False

; player_voice_model
;   The voice model for the player character to use if 'Voice player input' is activated.
player_voice_model =

; conversation_summary_enabled
;   Whether to generate and save conversation summaries when conversations end.
;                           If enabled: Summaries will be generated and saved to let NPCs remember past conversations.
;                           If disabled: No summaries will be generated when conversations end.
;                           Note: Summaries are still generated when a conversation exceeds the context window.
;   conversation_summary_enabled must either be 'True' or 'False'
;   default = True
conversation_summary_enabled = True

; enable_character_tag_reading
;   Enable tag-based bio expansion for characters. When enabled, comma-separated tags in the character CSV 'tags' column will be looked up in bio_templates.csv and appended to the character's bio.
;   enable_character_tag_reading must either be 'True' or 'False'
;   default = False
enable_character_tag_reading = False

; save_audio_data_to_character_folder
;   Whether to save audio data to an NPC's voice folder instead of MantellaVoice00.
;                           Enable this value if voicelines are not being played in-game.
;   save_audio_data_to_character_folder must either be 'True' or 'False'
;   default = False
save_audio_data_to_character_folder = False

; port
;   The port for the Mantella HTTP server to use.
;   port must be an integer between 0 and 65535
;   default = 4999
port = 4999

; show_http_debug_messages
;   Display the JSON going in and out of the server in Mantella.exe's log.
;   show_http_debug_messages must either be 'True' or 'False'
;   default = False
show_http_debug_messages = False

; advanced_logs
;   Save advanced logs to Mantella's My Games/Mantella/logging.log file.
;                           Useful for troubleshooting issues.
;   advanced_logs must either be 'True' or 'False'
;   default = False
advanced_logs = False

; enable_telemetry
;   Enable Open Telemetry for tracing. Note that a telemetry observability platform must also be configured.
;   enable_telemetry must either be 'True' or 'False'
;   default = False
enable_telemetry = False

; telemetry_otlp_endpoint
;   The endpoint for the Open Telemetry observability platform (eg Jaeger).
;   default = http://localhost:4318/v1/traces
telemetry_otlp_endpoint = http://localhost:4318/v1/traces

; telemetry_protocol
;   The protocol for Open Telemetry (can be 'http/protobuf' or 'grpc').
;   default = http/protobuf
telemetry_protocol = http/protobuf

